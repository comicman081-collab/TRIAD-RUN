# Delete Candidates

No files were deleted during this migration.

| Candidate | Reason | Action taken |
| --- | --- | --- |
| `D:\AI 종합 폴더\Games\TRIAD_RUN\assets\assets` | Duplicate nesting from the initial non-destructive asset staging copy. The correct `assets\characters` and `assets\battle_backgrounds` paths were subsequently populated. | None; retained for user review. |

The candidate is not referenced by the generated runtime entry.
